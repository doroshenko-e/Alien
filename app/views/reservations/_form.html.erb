<%= form_for ([@room, @room.reservations.new]) do |f| %>
	<%= f.date_field :start_date %>
	<%= f.date_field :end_date %>
	<%= f.hidden_field :room_id, value: @room.id %>
	<%= f.hidden_field :price, value: @room.price %> </br>
	<%= f.submit 'Book Now', class: 'btn btn-primary' %>

<% end %>